import React from 'react'
//import noteContext from '../context/notes/noteContext'

const About = () => {
 // const user = useContext(noteContext);

  // useEffect(() => {
  //   user.update()
  // }, [])
  return (
    <div className='mx-4'>
      <h3>About us </h3>
      {/* <div>{user.state.name} is a student from {user.state.class}</div> */}
    </div>
  )
}

export default About
